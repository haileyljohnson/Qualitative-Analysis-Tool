# Transcripts folder

Store your source transcript files (`.tsv`, `.csv`, `.xls`, or `.xlsx`) here that you want to use within the tool. The tool can also open files not stored within here. But I would recommend copying them into this file.
